{
  "name": "skillward-training",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "test:e2e:local": "playwright test --config=playwright.local.config.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.55.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.1",
    "esbuild": "0.25.9"
  }
}
